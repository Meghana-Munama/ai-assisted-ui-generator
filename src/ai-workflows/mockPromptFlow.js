const mockPromptFlow = {
  input: 'Create a responsive profile card with title, description, and action button.',
  detectedComponents: ['Card', 'Heading', 'Paragraph', 'Button'],
  output: 'Reusable React component structure'
};

export default mockPromptFlow;
