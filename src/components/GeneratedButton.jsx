import React from 'react';

const GeneratedButton = ({ label }) => {
  return (
    <button className="px-5 py-3 bg-brand-600 text-white rounded-xl font-medium hover:bg-brand-700 transition">
      {label}
    </button>
  );
};

export default GeneratedButton;
